# template-python

## setup

* modify `pyproject.toml`
  * set `{project_name}` 
  * set `{your_name}` 
  * set `{your_email}`
  
* modify `Makefile`
  * set `{your_module}`
* type `$ poetry install`
